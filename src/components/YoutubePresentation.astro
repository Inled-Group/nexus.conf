---
---


<section class="video-hero-section">
  <div class="video-hero-box">
    <div class="video-hero-content">
      <h2 class="video-hero-title">
        Puedes ver la presentación de novedades desde aquí
      </h2>
      <div class="video-hero-wrapper">
        <iframe width="560" height="315" src="https://www.youtube.com/embed/ndw0MdlXLho?si=DmBm2DjRoo1IKt1h" title="Presentación de novedades" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe>
      </div>
    </div>
    <div class="video-hero-box-border"></div>
  </div>
</section>

<style>
  .video-hero-section {
    width: 100vw;
    display: flex;
    align-items: center;
    justify-content: center;
    position: relative;
    padding: 7rem 0 7rem 0;
    background: none;
    overflow: hidden;
  }
  .video-hero-box {
    position: relative;
    width: 100vw;
    max-width: 1200px;
    min-height: 600px;
    background: rgba(30, 30, 30, 0.7);
    border-radius: 40px;
    box-shadow: 0 0 60px 10px rgba(255,255,255,0.3);
    display: flex;
    align-items: center;
    justify-content: center;
    z-index: 2;
    overflow: visible;
    padding: 3rem 2rem;
  }
  .video-hero-box-border {
    position: absolute;
    inset: -10px;
    border-radius: 48px;
    border: 3px solid rgba(255,255,255,0.7);
    box-shadow: 0 0 40px 10px rgba(255,255,255,0.5);
    pointer-events: none;
    animation: border-move 4s linear infinite;
    z-index: 3;
  }
  @keyframes border-move {
    0% { box-shadow: 0 0 40px 10px rgba(255,255,255,0.5); }
    25% { box-shadow: 0 0 80px 20px rgba(255,255,255,0.7); }
    50% { box-shadow: 0 0 40px 10px rgba(255,255,255,0.5); }
    75% { box-shadow: 0 0 80px 20px rgba(255,255,255,0.7); }
    100% { box-shadow: 0 0 40px 10px rgba(255,255,255,0.5); }
  }
  .video-hero-content {
    width: 100%;
    height: 100%;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    text-align: center;
    z-index: 4;
  }
  .video-hero-title {
    font-family: 'Space Grotesk', sans-serif;
    font-size: 2.5rem;
    font-weight: 700;
    line-height: 1.1;
    margin-bottom: 32px;
    color: #fff;
    animation: fade-in-up 1s ease-out 0.3s both;
  }
  .video-hero-wrapper {
    position: relative;
    width: 100%;
    max-width: 900px;
    aspect-ratio: 16/9;
    min-height: 400px;
    border-radius: 1rem;
    overflow: hidden;
    box-shadow: 0 2px 16px rgba(0,0,0,0.12);
    margin: 0 auto;
  }
  .video-hero-wrapper iframe {
    width: 100%;
    height: 100%;
    min-height: 400px;
    border: none;
    border-radius: 1rem;
  }
  @keyframes fade-in-up {
    0% {
      transform: translateY(30px);
      opacity: 0;
    }
    100% {
      transform: translateY(0);
      opacity: 1;
    }
  }
  @media (max-width: 768px) {
    .video-hero-box {
      width: 100vw;
      min-height: 300px;
      border-radius: 24px;
      padding: 1rem 0.5rem;
    }
    .video-hero-title {
      font-size: 1.5rem;
    }
    .video-hero-wrapper {
      max-width: 100vw;
      min-height: 180px;
      border-radius: 0.5rem;
    }
    .video-hero-wrapper iframe {
      min-height: 180px;
      border-radius: 0.5rem;
    }
  }
</style>
